---
- name: ANSISTRANO | Change symlink from current release to a previous one
  file:
    state: link
    path: "{{ ansistrano_current_path }}"
    src: "{{ (ansistrano_releases_path.stdout ~ '/' ~ ansistrano_rollback_release_version) | relpath(ansistrano_current_path | dirname) }}"
